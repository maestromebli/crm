"use client";

/**
 * Заготовка під інтеграції каталогів постачальників (Viyar, локальні прайси).
 * Зараз: ручний ввід у полі «Постачальник / код» у рядку + збереження в metadataJson.supplier.
 */
export function MaterialPickerPlaceholder() {
  return null;
}
