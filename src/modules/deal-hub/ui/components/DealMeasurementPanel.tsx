import { DealPlaceholderPanel } from "./_shared";

export function DealMeasurementPanel() {
  return (
    <DealPlaceholderPanel
      title="Заміри"
      text="Графік виїздів, розміри, фото та попередження про вплив на вартість."
    />
  );
}
