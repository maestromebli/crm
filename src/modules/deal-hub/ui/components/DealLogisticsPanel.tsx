import { DealPlaceholderPanel } from "./_shared";

export function DealLogisticsPanel() {
  return (
    <DealPlaceholderPanel
      title="Логістика"
      text="Адреса, обмеження, слот доставки та підтвердження передачі."
    />
  );
}
