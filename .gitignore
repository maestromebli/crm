# Dependencies
node_modules
.pnp
.pnp.js

# Next.js
.next
out

# Env & secrets (шаблон .env.example залишаємо)
.env
.env.local
.env.*.local

# Test / Playwright
test-results
playwright-report
blob-report

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# Vercel
.vercel

# TypeScript / збірка
*.tsbuildinfo
tsc.log
**/dist/

# Локальні завантаження CRM (контракти тощо) — лише .gitkeep у репозиторії
storage/uploads/**
!storage/uploads/.gitkeep

# Локальні backup-бандли та версійні знімки
backups/

# Monorepo на pnpm: не тримаємо npm lock-файли
package-lock.json

# Local / temp dirs (розгортання, тимчасові витяжки)
.tmp-*/
.tmp-*

# IDE (опційно; розкоментуйте якщо не хочете шарити)
# .vscode
